from django.conf import settings

REC_CONFIG = {
  'image_dir': settings.MEDIA_ROOT / 'rec'
}

VQA_CONFIG = {
  
}

BASE_ROOT = settings.BASE_DIR
STATIC_ROOT = settings.STATIC_ROOT
MEDIA_ROOT = settings.MEDIA_ROOT
